INSERT INTO documents (
    company_id,
    warehouse_id,
    partner_id,
    created_by_user_id,
    document_type,
    status,
    posting_status,
    document_number,
    document_date,
    due_date,
    currency_code,
    exchange_rate,
    payment_method,
	  card_type,
    note,
    subtotal_amount,
    tax_amount,
    discount_amount,
    total_amount,
    paid_amount
)
VALUES (
    {{appsmith.store.companyId}},
    {{SelectWarehouse.text || null}},
    {{SelectPartner.selectedOptionValue || null}},
    {{appsmith.store.userId}},
    'SALES_INVOICE',
    'CONFIRMED',
    'READY',
    {{invoice_no.text}},
    {{ moment(InoiceDateInput.selectedDate).format("YYYY-MM-DD") }},
    {{dutedate.selectedDate || null}},
    'EUR',
    1.000000,
    {{SelectPayment.selectedOptionValue || 'DEFERRED'}},
	  {{CardTypeSelect.selectedOptionValue}},
    {{InvoiceNoteInput.text || null}},
    {{SubtotalInput.text || 0}},
    {{TaxAmountInput.text || 0}},
    {{DiscountAmountInput.text || 0}},
    {{TotalAmountInput.text || 0}},
    {{TotalAmountInput.text.text || 0}}
);