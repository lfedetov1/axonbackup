{
  "gitSyncId": "69e7d447c5e91666c08fb1a7_78a9e0e6-e17e-4d8f-abb5-3ff0963296a4",
  "id": "Sales_GetSalesDocumentPreviewTaxSumm",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n    COALESCE(tr.rate, 0) AS taxRate,\n\n    SUM(\n      CASE\n        WHEN d.document_type = 'CREDIT_NOTE'\n        THEN -ABS(COALESCE(di.line_subtotal, COALESCE(di.quantity, 0) * COALESCE(di.unit_price, 0), 0))\n        ELSE COALESCE(di.line_subtotal, COALESCE(di.quantity, 0) * COALESCE(di.unit_price, 0), 0)\n      END\n    ) AS taxableAmount,\n\n    SUM(\n      CASE\n        WHEN d.document_type = 'CREDIT_NOTE' THEN -ABS(COALESCE(di.tax_amount, 0))\n        ELSE COALESCE(di.tax_amount, 0)\n      END\n    ) AS taxAmount,\n\n    SUM(\n      CASE\n        WHEN d.document_type = 'CREDIT_NOTE' THEN -ABS(COALESCE(di.line_total, 0))\n        ELSE COALESCE(di.line_total, 0)\n      END\n    ) AS totalAmount\n\nFROM documents d\nJOIN document_items di ON di.document_id = d.id\nLEFT JOIN tax_rates tr ON tr.id = di.tax_rate_id\nWHERE d.id = {{ Number((this.params && this.params.documentId) || appsmith.store.salesSelectedDocumentId || 0) }}\n  AND d.company_id = {{ Number(appsmith.store.companyId || 1) }}\n  AND d.document_type IN ('QUOTE', 'SALES_ORDER', 'DELIVERY_NOTE', 'SALES_INVOICE', 'CREDIT_NOTE')\nGROUP BY COALESCE(tr.rate, 0)\nORDER BY COALESCE(tr.rate, 0);",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Axon",
      "isAutoGenerated": false,
      "name": "Axon",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "GetSalesDocumentPreviewTaxSumm",
    "pageId": "Sales",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}