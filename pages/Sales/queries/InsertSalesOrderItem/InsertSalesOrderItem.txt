INSERT INTO document_items (
    document_id,
    line_no,
    product_id,
    description,
    quantity,
    reserved_quantity,
    unit_id,
    unit_price,
    discount_amount,
    line_subtotal,
    tax_rate_id,
    tax_amount,
    line_total,
    note
)
VALUES (
    {{this.params.documentId}},
    {{this.params.lineNo}},
    {{this.params.productId}},
    {{this.params.description || null}},
    {{this.params.quantity}},
    0,
    {{this.params.unitId || null}},
    {{this.params.unitPrice || 0}},
    {{this.params.discountAmount || 0}},
    {{this.params.lineSubtotal || 0}},
    {{this.params.taxRateId || null}},
    {{this.params.taxAmount || 0}},
    {{this.params.lineTotal || 0}},
    {{this.params.note || null}}
);