{
  "gitSyncId": "69e7d447c5e91666c08fb1a7_a564cc1a-3472-4163-a060-27cdc8c68d99",
  "id": "Sales_InsertPackageForDeliveryNote",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "INSERT INTO document_packages (\n    company_id,\n    document_id,\n    source_document_id,\n    package_number,\n    package_barcode,\n    package_type,\n    package_status,\n    warehouse_id,\n    partner_id,\n    item_count,\n    total_quantity,\n    note,\n    created_by_user_id\n)\nSELECT\n    d.company_id,\n    d.id,\n    d.source_document_id,\n    {{ this.params.packageNumber }},\n    {{ this.params.packageBarcode }},\n    {{ this.params.packageType || \"BOX\" }},\n    'PACKED',\n    d.warehouse_id,\n    d.partner_id,\n    COUNT(di.id),\n    COALESCE(SUM(di.quantity), 0),\n    CONCAT('Package created from delivery note ', d.document_number),\n    {{ Number(appsmith.store.userId || 0) }}\nFROM documents d\nLEFT JOIN document_items di ON di.document_id = d.id\nWHERE d.id = {{ Number(this.params.deliveryNoteId || 0) }}\n  AND d.company_id = {{ Number(appsmith.store.companyId || 0) }}\n  AND d.document_type = 'DELIVERY_NOTE'\nGROUP BY\n    d.company_id,\n    d.id,\n    d.source_document_id,\n    d.warehouse_id,\n    d.partner_id,\n    d.document_number;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Axon",
      "isAutoGenerated": false,
      "name": "Axon",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "InsertPackageForDeliveryNote",
    "pageId": "Sales",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}