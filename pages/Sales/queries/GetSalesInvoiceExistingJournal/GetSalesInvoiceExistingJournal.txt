SELECT
    id AS journalEntryId,
    entry_number AS entryNumber
FROM journal_entries
WHERE company_id = {{ Number(appsmith.store.companyId || 1) }}
  AND reference_number = {{ this.params.documentNumber || "" }}
  AND document_type = 'SALES_INVOICE'
LIMIT 1;