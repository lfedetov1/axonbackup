INSERT INTO documents (
    company_id,
    warehouse_id,
    partner_id,
    created_by_user_id,
    document_type,
    status,
    posting_status,
    sales_channel,
    document_number,
    customer_reference,
    internal_reference,
    document_date,
    due_date,
    valid_until,
    currency_code,
    exchange_rate,
    note,
    subtotal_amount,
    tax_amount,
    discount_amount,
    total_amount
)
VALUES (
    {{ appsmith.store.companyId }},
    {{ appsmith.store.warehouseId1 || 1 }},
    {{ SelectPartner1.selectedOptionValue || null }},
    {{ appsmith.store.userId }},
    'QUOTE',
    {{ QuotationStatusSelect.selectedOptionValue || "DRAFT" }},
    'NOT_POSTED',
    {{ QuotationSalesChannelSelect.selectedOptionValue || "BACKOFFICE" }},
    {{ Quotations_no.text }},
    {{ QuotationCustomerReferenceInpu.text || null }},
    {{ QuotationInternalReferenceInpu.text || null }},
    {{ dutedate1.selectedDate ? moment(dutedate1.selectedDate).format("YYYY-MM-DD") : moment().format("YYYY-MM-DD") }},
    null,
    {{ QuotationValidUntilInput.selectedDate ? moment(QuotationValidUntilInput.selectedDate).format("YYYY-MM-DD") : null }},
    {{ QuotationCurrencySelect.selectedOptionValue || "EUR" }},
    1.000000,
    {{ Note.text || null }},
    {{ SubtotalInput1.text || 0 }},
    {{ TaxAmountInput1.text || 0 }},
    {{ DiscountAmountInput1.text || 0 }},
    {{ TotalAmountInput1.text || 0 }}
);
