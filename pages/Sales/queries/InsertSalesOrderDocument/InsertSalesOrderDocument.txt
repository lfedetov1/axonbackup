INSERT INTO documents (
    company_id,
    document_type,
    document_number,
    document_date,
    due_date,
    partner_id,
    warehouse_id,
    currency_code,
    exchange_rate,
    subtotal_amount,
    discount_amount,
    tax_amount,
    total_amount,
    paid_amount,
    status,
    posting_status,
    sales_channel,
    fulfillment_type,
    note,
    created_by_user_id
)
VALUES (
    {{ Number(appsmith.store.companyId || 0) }},
    'SALES_ORDER',
    {{ SalesOrderNumberInput.text }},
    {{ moment(SalesOrderDateInput.selectedDate || moment()).format("YYYY-MM-DD") }},
    {{ SalesOrderDueDateInput.selectedDate ? moment(SalesOrderDueDateInput.selectedDate).format("YYYY-MM-DD") : null }},
    {{ Number(SalesOrderCustomerSelect.selectedOptionValue || 0) }},
    {{ Number(SalesOrderWarehouseSelect.selectedOptionValue || 0) }},
    'EUR',
    1,
    {{ Number(this.params.subtotalAmount || 0) }},
    {{ Number(this.params.discountAmount || 0) }},
    {{ Number(this.params.taxAmount || 0) }},
    {{ Number(this.params.totalAmount || 0) }},
    0,
    {{ this.params.status || "DRAFT" }},
    'NOT_POSTED',
    {{ SalesOrderChannelSelect.selectedOptionValue || "STORE" }},
    {{ SalesOrderFulfillmentSelect.selectedOptionValue || "PICKUP" }},
    {{ SalesOrderNoteInput.text || null }},
    {{ Number(appsmith.store.userId || 0) }}
);