INSERT INTO documents (
    company_id,
    document_type,
    document_number,
    document_date,
    due_date,
    partner_id,
    warehouse_id,
    payment_method,
    currency_code,
    exchange_rate,
    subtotal_amount,
    discount_amount,
    tax_amount,
    total_amount,
    paid_amount,
    status,
    posting_status,
    note,
    created_by_user_id
)
VALUES (
    {{ Number(appsmith.store.companyId || 1) }},
    {{ this.params.documentType || "SALES_INVOICE" }},
    {{ this.params.documentNumber }},
    {{ this.params.documentDate }},
    {{ this.params.dueDate || this.params.documentDate }},
    {{ Number(this.params.customerId || 0) }},
    {{ Number(this.params.warehouseId || 0) }},
    {{ this.params.paymentMethod || null }},
    {{ this.params.currencyCode || "EUR" }},
    {{ Number(this.params.exchangeRate || 1) }},
    {{ Number(this.params.subtotalAmount || 0) }},
    {{ Number(this.params.discountAmount || 0) }},
    {{ Number(this.params.taxAmount || 0) }},
    {{ Number(this.params.totalAmount || 0) }},
    0,
    {{ this.params.status || "DRAFT" }},
    'NOT_POSTED',
    {{ this.params.note || null }},
    {{ Number(appsmith.store.userId || 0) }}
);