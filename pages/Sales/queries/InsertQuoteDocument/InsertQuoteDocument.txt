INSERT INTO documents (
    company_id,
    document_type,
    document_number,
    document_date,
    valid_until,
    status,
    posting_status,
    partner_id,
    warehouse_id,
    currency_code,
    sales_channel,
    subtotal_amount,
    discount_amount,
    tax_amount,
    total_amount,
    note,
    created_by_user_id,
    created_at
)
VALUES (
    {{ Number(appsmith.store.companyId || 0) }},
    'QUOTE',
    {{ QuoteNumberInput.text || "" }},
    {{ moment(QuoteDateInput.selectedDate || QuoteDateInput.text || moment()).format("YYYY-MM-DD") }},
    {{ moment(QuoteValidUntilInput.selectedDate || QuoteValidUntilInput.text || moment().add(14, "days")).format("YYYY-MM-DD") }},
    {{ this.params.status || "DRAFT" }},
    'NOT_POSTED',
    {{ QuoteCustomerSelect.selectedOptionValue || null }},
    {{ QuoteWarehouseSelect.selectedOptionValue || null }},
    {{ this.params.currencyCode || "EUR" }},
    {{ QuoteSalesChannelSelect.selectedOptionValue || "STORE" }},
    {{ Number(this.params.subtotalAmount || 0) }},
    {{ Number(this.params.discountAmount || 0) }},
    {{ Number(this.params.taxAmount || 0) }},
    {{ Number(this.params.totalAmount || 0) }},
    {{ QuoteNoteInput.text || null }},
    {{ Number(appsmith.store.userId || 0) }},
    NOW()
);