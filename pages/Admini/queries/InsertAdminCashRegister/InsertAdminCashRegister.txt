INSERT INTO cash_registers (
  company_id,
  code,
  fiscal_business_space_code,
  fiscal_device_code,
  name,
  currency_code,
  warehouse_id,
  responsible_user_id,
  opening_balance,
  is_active
)
VALUES (
  {{ Number(appsmith.store.companyId || 1) }},
  {{ AdminCashRegisterCodeInput.text.trim().toUpperCase() }},
  {{ AdminCashRegisterBusinessSpace.text.trim() || null }},
  {{ AdminCashRegisterFiscalDeviceI.text.trim() || null }},
  {{ AdminCashRegisterNameInput.text.trim() }},
  {{ AdminCashRegisterCurrencyInput.text.trim().toUpperCase() || "EUR" }},
  {{ Number(AdminCashRegisterWarehouseSele.selectedOptionValue || 0) || null }},
  {{ Number(AdminCashRegisterResponsibleUs.selectedOptionValue || 0) || null }},
  {{ Number(AdminCashRegisterOpeningBalanc.text || 0) }},
  {{ AdminCashRegisterActiveSwitch.isSwitchedOn ? 1 : 0 }}
);