UPDATE documents
SET status = 'PACKED', updated_at = NOW()
WHERE id = {{this.params.documentId}}
  AND company_id = {{appsmith.store.companyId}}
  AND document_type IN ('DELIVERY_NOTE', 'STOCK_OUT', 'TRANSFER_REQUEST')
  AND status = 'PICKED';
