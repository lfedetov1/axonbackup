INSERT INTO documents (
    company_id,
    warehouse_id,
    created_by_user_id,
    document_type,
    status,
    document_number,
    document_date,
    currency_code,
    note,
    subtotal_amount,
    tax_amount,
    discount_amount,
    total_amount
)
VALUES (
    {{appsmith.store.companyId}},
    {{this.params.warehouseId}},
    {{appsmith.store.userId}},
    'STOCK_OUT',
    {{this.params.status}},
    {{StockIssueNumberInput.text.trim()}},
    {{this.params.issueDate}},
    'EUR',
    {{this.params.note || null}},
    {{this.params.totalAmount || 0}},
    0,
    0,
    {{this.params.totalAmount || 0}}
);
