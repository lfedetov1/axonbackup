SELECT * FROM accounting_accounts LIMIT 10;INSERT INTO document_items (
    document_id,
    line_no,
    product_id,
    description,
    unit_id,
    warehouse_id,
    quantity,
    system_quantity,
    counted_quantity,
    variance_quantity,
    variance_value,
    unit_price,
    line_subtotal,
    line_total,
    counted_by_user_id,
    counted_at,
    warehouse_location_id,
    note
)
VALUES (
    {{this.params.documentId}},
    {{this.params.lineNo}},
    {{this.params.productId}},
    {{this.params.description || null}},
    {{this.params.unitId}},
    {{this.params.warehouseId}},
    {{this.params.countedQuantity || 0}},
    {{this.params.systemQuantity || 0}},
    {{this.params.countedQuantity || 0}},
    {{this.params.varianceQuantity || 0}},
    {{this.params.varianceValue || 0}},
    {{this.params.unitCost || 0}},
    {{this.params.varianceValue || 0}},
    {{this.params.varianceValue || 0}},
    {{appsmith.store.userId}},
    NOW(),
    {{this.params.warehouseLocationId || null}},
    {{this.params.note || null}}
);
