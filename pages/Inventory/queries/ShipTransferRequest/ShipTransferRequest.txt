UPDATE documents
SET status = 'SHIPPED', posting_status = 'POSTED', updated_at = NOW()
WHERE id = {{this.params.documentId}}
  AND company_id = {{appsmith.store.companyId}}
  AND document_type = 'TRANSFER_REQUEST'
  AND status IN ('PICKED', 'DRAFT', 'REQUESTED');
