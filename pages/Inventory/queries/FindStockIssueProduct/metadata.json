{
  "gitSyncId": "69e7d447c5e91666c08fb1a7_200153ef-7818-4622-aae8-102fb0961c48",
  "id": "Inventory_FindStockIssueProduct",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n    p.id AS productId,\n    p.code AS productCode,\n    p.name AS productName,\n    p.barcode AS barcode,\n    p.sku AS sku,\n    p.unit_id AS unitId,\n    u.code AS unitCode,\n    p.purchase_price AS purchasePrice,\n    p.track_stock AS trackStock,\n    COALESCE(ib.quantity_balance, 0) AS currentStock\nFROM products p\nLEFT JOIN units_of_measure u ON u.id = p.unit_id\nLEFT JOIN v_inventory_balance ib\n  ON ib.company_id = p.company_id\n AND ib.product_id = p.id\n AND ib.warehouse_id = {{this.params.warehouseId}}\nWHERE p.is_active = 1\n  AND p.track_stock = 1\n  AND (\n      p.code = TRIM({{this.params.lookup}})\n      OR p.barcode = TRIM({{this.params.lookup}})\n      OR p.sku = TRIM({{this.params.lookup}})\n      OR p.name LIKE CONCAT('%', TRIM({{this.params.lookup}}), '%')\n  )\nORDER BY\n    CASE\n        WHEN p.barcode = TRIM({{this.params.lookup}}) THEN 1\n        WHEN p.code = TRIM({{this.params.lookup}}) THEN 2\n        WHEN p.sku = TRIM({{this.params.lookup}}) THEN 3\n        ELSE 4\n    END,\n    p.name ASC\nLIMIT 1;\n",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Axon",
      "isAutoGenerated": false,
      "name": "Axon",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "FindStockIssueProduct",
    "pageId": "Inventory",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}