SELECT
    d.id AS inventoryCountId,
    d.document_number AS inventoryCountNumber,
    d.document_date AS inventoryCountDate,
    d.status AS status,
    d.warehouse_id AS warehouseId,
    w.code AS warehouseCode,
    w.name AS warehouseName,
    d.note AS note
FROM documents d
LEFT JOIN warehouses w ON w.id = d.warehouse_id
WHERE d.company_id = {{appsmith.store.companyId}}
  AND d.document_type = 'INVENTORY_COUNT'
  AND d.created_by_user_id = {{appsmith.store.userId}}
ORDER BY d.id DESC
LIMIT 1;
