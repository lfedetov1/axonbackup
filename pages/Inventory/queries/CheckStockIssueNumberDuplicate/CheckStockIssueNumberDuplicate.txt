SELECT
    id AS issueId,
    document_number AS issueNumber,
    status AS status
FROM documents
WHERE company_id = {{appsmith.store.companyId}}
  AND document_type = 'STOCK_OUT'
  AND document_number = {{StockIssueNumberInput.text.trim()}}
LIMIT 1;
