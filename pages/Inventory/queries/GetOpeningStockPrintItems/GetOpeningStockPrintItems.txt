SELECT
    di.id AS documentItemId,
    di.line_no AS lineNo,

    p.id AS productId,
    p.code AS productCode,
    p.name AS productName,
    p.barcode AS barcode,
    p.sku AS sku,

    u.code AS unitCode,

    di.quantity AS quantity,
    di.unit_price AS unitCost,
    di.line_total AS lineTotal,

    di.batch_number AS batchNumber,
    di.serial_number AS serialNumber,
    di.expiry_date AS expiryDate,
    di.note AS note

FROM document_items di
JOIN products p ON p.id = di.product_id
LEFT JOIN units_of_measure u ON u.id = di.unit_id

WHERE di.document_id = {{this.params.documentId || appsmith.store.currentOpeningStockId || OpeningStockTable.selectedRow.documentId || 0}}

ORDER BY di.line_no;
