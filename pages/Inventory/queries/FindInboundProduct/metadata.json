{
  "gitSyncId": "69e7d447c5e91666c08fb1a7_0b2b91b9-cd41-46df-996b-f7fad6d221ed",
  "id": "Inventory_FindInboundProduct",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n    p.id AS productId,\n    p.code AS productCode,\n    p.name AS productName,\n    p.barcode AS barcode,\n    p.sku AS sku,\n    p.description AS description,\n    p.purchase_price AS purchasePrice,\n    p.track_stock AS trackStock,\n    u.id AS unitId,\n    u.code AS unitCode,\n    u.name AS unitName\nFROM products p\nLEFT JOIN units_of_measure u\n  ON u.id = p.unit_id\nWHERE p.company_id = {{appsmith.store.companyId}}\n  AND p.is_active = 1\n  AND (\n      p.code = TRIM({{this.params.lookup}})\n      OR p.barcode = TRIM({{this.params.lookup}})\n      OR p.sku = TRIM({{this.params.lookup}})\n      OR p.name LIKE CONCAT('%', TRIM({{this.params.lookup}}), '%')\n  )\nORDER BY\n    CASE\n      WHEN p.barcode = TRIM({{this.params.lookup}}) THEN 1\n      WHEN p.code = TRIM({{this.params.lookup}}) THEN 2\n      WHEN p.sku = TRIM({{this.params.lookup}}) THEN 3\n      ELSE 4\n    END,\n    p.name\nLIMIT 1;\n",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Axon",
      "isAutoGenerated": false,
      "name": "Axon",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "FindInboundProduct",
    "pageId": "Inventory",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}