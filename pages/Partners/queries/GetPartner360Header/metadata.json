{
  "gitSyncId": "69e7d447c5e91666c08fb1a7_5e6a9ac8-c9d9-40a1-b0a0-febcd222a00c",
  "id": "Partners_GetPartner360Header",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n    bp.id AS partnerId,\n    bp.company_id AS companyId,\n    bp.code AS partnerCode,\n    bp.name AS partnerName,\n    bp.legal_name AS legalName,\n    bp.contact_person AS contactPerson,\n    bp.tax_number AS taxNumber,\n    bp.registration_number AS registrationNumber,\n\n    bp.email,\n    bp.phone,\n    bp.mobile,\n    bp.address_line1 AS address,\n    bp.city_name AS city,\n    bp.postal_code AS postalCode,\n    bp.payment_term_days AS paymentTermDays,\n    bp.credit_limit AS creditLimit,\n\n    bp.is_active AS isActive,\n\n    COALESCE(fin.documentCount, 0) AS documentCount,\n    COALESCE(fin.salesAmount, 0) AS salesAmount,\n    COALESCE(fin.purchaseAmount, 0) AS purchaseAmount,\n    COALESCE(fin.openAmount, 0) AS openAmount,\n    fin.lastDocumentDate AS lastDocumentDate,\n\n    CASE\n        WHEN bp.is_active = 0 THEN 'INACTIVE'\n        WHEN COALESCE(fin.openAmount, 0) > 0 THEN 'OPEN_BALANCE'\n        WHEN COALESCE(fin.documentCount, 0) = 0 THEN 'NEW'\n        ELSE 'ACTIVE'\n    END AS riskStatus\n\nFROM business_partners bp\n\nLEFT JOIN (\n    SELECT\n        d.partner_id,\n        COUNT(d.id) AS documentCount,\n        MAX(d.document_date) AS lastDocumentDate,\n\n        SUM(\n            CASE\n                WHEN d.document_type IN (\n                    'SALES_INVOICE',\n                    'POS_SALE',\n                    'SALES_RECEIPT'\n                )\n                THEN COALESCE(d.total_amount, 0)\n                ELSE 0\n            END\n        ) AS salesAmount,\n\n        SUM(\n            CASE\n                WHEN d.document_type IN (\n                    'PURCHASE_INVOICE',\n                    'PURCHASE_RECEIPT'\n                )\n                THEN COALESCE(d.total_amount, 0)\n                ELSE 0\n            END\n        ) AS purchaseAmount,\n\n        SUM(\n            CASE\n                WHEN d.document_type IN (\n                    'SALES_INVOICE',\n                    'POS_SALE',\n                    'SALES_RECEIPT'\n                )\n                THEN COALESCE(d.total_amount, 0) - COALESCE(d.paid_amount, 0)\n\n                WHEN d.document_type = 'CREDIT_NOTE'\n                THEN -ABS(\n                    COALESCE(d.total_amount, 0) -\n                    COALESCE(d.paid_amount, 0)\n                )\n\n                ELSE 0\n            END\n        ) AS openAmount\n\n    FROM documents d\n    WHERE d.partner_id = {{ Number(appsmith.store.selectedPartnerId || 0) }}\n      AND d.status <> 'CANCELLED'\n    GROUP BY d.partner_id\n) fin ON fin.partner_id = bp.id\n\nWHERE bp.id = {{ Number(appsmith.store.selectedPartnerId || 0) }}\n  AND (\n      {{ Number(appsmith.store.selectedPartnerCompanyId || appsmith.store.companyId || 0) }} = 0\n      OR bp.company_id = {{ Number(appsmith.store.selectedPartnerCompanyId || appsmith.store.companyId || 0) }}\n  )\n\nLIMIT 1;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Axon",
      "isAutoGenerated": false,
      "name": "Axon",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "GetPartner360Header",
    "pageId": "Partners",
    "runBehaviour": "AUTOMATIC",
    "userSetOnLoad": false
  }
}