INSERT INTO cash_register_closings (
    company_id,
    cash_register_id,
    closing_date,
    opened_at,
    closed_at,
    opened_by_user_id,
    closed_by_user_id,
    opening_amount,
    expected_cash_amount,
    counted_cash_amount,
    difference_amount,
    cash_sales_amount,
    card_sales_amount,
    bank_sales_amount,
    other_sales_amount,
    cash_in_amount,
    cash_out_amount,
    transaction_count,
    status,
    note
)
VALUES (
    {{appsmith.store.companyId}},
    {{PosControlCashRegisterSelect.selectedOptionValue}},
    {{moment().format("YYYY-MM-DD")}},
    {{appsmith.store.posShiftStatus?.openedAt || null}},
    {{moment().format("YYYY-MM-DD HH:mm:ss")}},
    {{appsmith.store.userId}},
    {{appsmith.store.userId}},
    {{Number(appsmith.store.posShiftStatus?.openingAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.expectedCashAmount || 0)}},
    {{Number(appsmith.store.posCloseCashTotal || 0)}},
    {{Number(appsmith.store.posCloseCashTotal || 0) - Number(appsmith.store.posShiftStatus?.expectedCashAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.cashSalesAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.cardSalesAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.bankSalesAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.otherSalesAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.cashInAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.cashOutAmount || 0)}},
    {{Number(appsmith.store.posShiftStatus?.receiptCount || 0)}},
    'CLOSED',
    {{PosControlClosingNoteInput.text || null}}
);
