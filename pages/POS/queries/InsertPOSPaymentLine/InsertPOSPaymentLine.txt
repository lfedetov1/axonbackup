INSERT INTO pos_payment_lines (
    company_id,
    document_id,
    payment_method_id,
    payment_method_code,
    method_group,
    amount,
    received_amount,
    change_amount,
    reference_number,
    card_type,
    note,
    created_by_user_id
)
VALUES (
    {{appsmith.store.companyId}},
    {{this.params.documentId}},
    {{this.params.paymentMethodId || null}},
    {{this.params.paymentMethodCode}},
    {{this.params.methodGroup}},
    {{this.params.amount}},
    {{this.params.receivedAmount || this.params.amount}},
    {{this.params.changeAmount || 0}},
    {{this.params.referenceNumber || null}},
    {{this.params.cardType || null}},
    {{this.params.note || null}},
    {{appsmith.store.userId}}
);