INSERT INTO documents (
    company_id,
    warehouse_id,
    partner_id,
    created_by_user_id,
    approved_by_user_id,
    cashier_user_id,
    document_type,
    status,
    posting_status,
    sales_channel,
    payment_method,
    card_type,
    document_number,
    document_date,
    due_date,
    currency_code,
    exchange_rate,
    source_document_id,
    return_reason_id,
    note,
    subtotal_amount,
    tax_amount,
    discount_amount,
    total_amount,
    paid_amount
)
VALUES (
    {{appsmith.store.companyId}},
    {{this.params.sourceInvoice.warehouseId || appsmith.store.warehouseId}},
    {{this.params.sourceInvoice.partnerId || null}},
    {{appsmith.store.userId}},
    {{this.params.managerUserId}},
    {{appsmith.store.userId}},
    'SALES_RETURN',
    'POSTED',
    'POSTED',
    'POS',
    {{this.params.paymentMethod}},
    {{this.params.cardType || null}},
    CONCAT('RET-', DATE_FORMAT(NOW(), '%Y%m%d%H%i%s')),
    CURDATE(),
    CURDATE(),
    'EUR',
    1,
    {{this.params.sourceInvoice.documentId}},
    {{ReturnReasonSelect.selectedOptionValue}},
    {{ReturnNoteInput.text || null}},
    {{this.params.totals.subtotal}},
    {{this.params.totals.tax}},
    {{this.params.totals.discount}},
    {{this.params.totals.total}},
    {{this.params.totals.total}}
);