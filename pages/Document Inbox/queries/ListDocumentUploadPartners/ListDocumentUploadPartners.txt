SELECT
    id AS value,
    CONCAT(code, ' - ', name) AS label,
    email,
    phone
FROM business_partners
WHERE company_id = {{ Number(appsmith.store.companyId || 1) }}
  AND is_active = 1
ORDER BY name;