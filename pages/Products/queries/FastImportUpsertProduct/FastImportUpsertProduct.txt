INSERT INTO products (
    company_id,
    category_id,
    unit_id,
    tax_rate_id,
    code,
    name,
    short_name,
    description,
    barcode,
    sku,
    hs_code,
    country_of_origin_id,
    customs_description,
    intrastat_code,
    product_type,
    track_stock,
    is_used_item,
    serial_number_required,
    minimum_stock,
    maximum_stock,
    reorder_level,
    purchase_price,
    sale_price,
    weight_kg,
    net_weight_kg,
    gross_weight_kg,
    note,
    is_active
)
VALUES (
    {{appsmith.store.companyId}},
    {{this.params.categoryId || null}},
    {{this.params.unitId}},
    {{this.params.taxRateId || null}},
    {{this.params.row.code}},
    {{this.params.row.name}},
    {{this.params.row.short_name || null}},
    {{this.params.row.description || null}},
    {{this.params.row.barcode || null}},
    {{this.params.row.sku || null}},
    {{this.params.row.hs_code || null}},
    {{this.params.countryId || null}},
    {{this.params.row.customs_description || null}},
    {{this.params.row.intrastat_code || null}},
    {{this.params.row.product_type || 'GOODS'}},
    {{String(this.params.row.product_type || 'GOODS').toUpperCase() === 'SERVICE' ? 0 : Number(this.params.row.track_stock || 1)}},
    {{Number(this.params.row.is_used_item || 0)}},
    {{Number(this.params.row.serial_number_required || 0)}},
    {{this.params.row.minimum_stock || 0}},
    {{this.params.row.maximum_stock || null}},
    {{this.params.row.reorder_level || null}},
    {{this.params.row.purchase_price || 0}},
    {{this.params.row.sale_price || 0}},
    {{this.params.row.weight_kg || null}},
    {{this.params.row.net_weight_kg || null}},
    {{this.params.row.gross_weight_kg || null}},
    {{this.params.row.note || null}},
    {{Number(this.params.row.is_active || 1)}}
)
ON DUPLICATE KEY UPDATE
    category_id = VALUES(category_id),
    unit_id = VALUES(unit_id),
    tax_rate_id = VALUES(tax_rate_id),
    name = VALUES(name),
    short_name = VALUES(short_name),
    description = VALUES(description),
    barcode = VALUES(barcode),
    sku = VALUES(sku),
    hs_code = VALUES(hs_code),
    country_of_origin_id = VALUES(country_of_origin_id),
    customs_description = VALUES(customs_description),
    intrastat_code = VALUES(intrastat_code),
    product_type = VALUES(product_type),
    track_stock = VALUES(track_stock),
    is_used_item = VALUES(is_used_item),
    serial_number_required = VALUES(serial_number_required),
    minimum_stock = VALUES(minimum_stock),
    maximum_stock = VALUES(maximum_stock),
    reorder_level = VALUES(reorder_level),
    purchase_price = VALUES(purchase_price),
    sale_price = VALUES(sale_price),
    weight_kg = VALUES(weight_kg),
    net_weight_kg = VALUES(net_weight_kg),
    gross_weight_kg = VALUES(gross_weight_kg),
    note = VALUES(note),
    is_active = VALUES(is_active),
    updated_at = NOW();
