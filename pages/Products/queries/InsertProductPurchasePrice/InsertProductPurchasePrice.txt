INSERT INTO product_prices (
    product_id,
    price_type,
    currency_code,
    price,
    valid_from,
    valid_to
)
VALUES (
    {{ this.params.productId }},
    'PURCHASE',
    {{ ProductCurrencySelect.selectedOptionValue || 'EUR' }},
    {{ ProductPurchasePriceInput.text || 0 }},
    {{ ProductPriceValidFromInput.selectedDate
        ? moment(ProductPriceValidFromInput.selectedDate).format("YYYY-MM-DD")
        : moment().format("YYYY-MM-DD") }},
    NULL
);
