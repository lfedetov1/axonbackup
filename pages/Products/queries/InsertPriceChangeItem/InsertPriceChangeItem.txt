INSERT INTO product_price_change_items (
    price_change_record_id,
    product_id,
    currency_code,
    source_price_id,
    old_price,
    new_price,
    lowest_price_30_days,
    discount_percent,
    promotion_end_date,
    restore_price,
    note
)
VALUES (
    {{ Number(this.params.priceChangeId || 0) }},
    {{ Number(this.params.productId || 0) }},
    {{ this.params.currencyCode || "EUR" }},
    {{ this.params.sourcePriceId || null }},
    {{ Number(this.params.oldPrice || 0) }},
    {{ Number(this.params.newPrice || 0) }},
    {{ Number(this.params.lowestPrice30Days || 0) }},
    {{ Number(this.params.discountPercent || 0) }},
    {{ this.params.promotionEndDate || null }},
    {{ this.params.restorePrice || null }},
    {{ this.params.note || null }}
);