INSERT INTO product_prices (
    product_id,
    price_type,
    currency_code,
    price,
    valid_from,
    valid_to
)
VALUES (
    {{ Number(this.params.productId || 0) }},
    'SALE',
    {{ this.params.currencyCode || "EUR" }},
    {{ Number(this.params.newPrice || 0) }},
    {{ this.params.effectiveDate }},
    {{ this.params.validTo || null }}
);